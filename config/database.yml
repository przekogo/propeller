default: &default 
  adapter: postgresql 
  encoding: unicode 
  database: propeller_db
  pool: 5
  username: kogo
  password:

development:
  <<: *default
  database: propeller_db

test:
  <<: *default 
  database: propeller_test
